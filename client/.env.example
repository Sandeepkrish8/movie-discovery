# Leave EMPTY for local development — Vite proxies /api to localhost:4000.
# In production set this to the deployed API origin, e.g.
#   VITE_API_BASE_URL=https://movie-discovery-api.onrender.com
VITE_API_BASE_URL=
